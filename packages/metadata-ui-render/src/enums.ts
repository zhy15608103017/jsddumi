export enum LoadStrategy  {
    ALL = 'ALL',
    PREFIX = 'PREFIX'
}

export enum DateElementType {
    BOOLEAN = 'BOOLEAN',
    DATETIME = 'DATETIME',
    FLOAT = 'FLOAT',
    INTEGER = 'INTEGER',
    STRING = 'STRING',
}